# Known issues

- `BLOCKED_BY_CREDENTIALS`: no credentialed live response has verified Jeju coverage, IDs, direction, route variants, or cadence.
- `BLOCKED_BY_CREDENTIALS`: APNs remote Live Activity updates need an Apple developer team, bundle identifiers, signing key, and device token flow.
- `BLOCKED_BY_PAID_MEMBERSHIP`: no paid Apple Developer Program team is reachable from this machine. Verified read-only on 2026-08-21: Xcode has exactly one Apple ID account, and it resolves to exactly one team — `89CGFQ24U5`, `Luca Kim (Personal Team)`, `teamType = Personal Team`, `isFreeProvisioningTeam = true`. The only keychain signing identity is an Apple Development certificate (`OU=89CGFQ24U5`, `O=Luca Kim`, valid 2026-07-09 → 2027-07-09); no Apple Distribution certificate and no provisioning profiles are installed. A free Personal Team cannot create App Store distribution certificates or profiles and cannot upload to TestFlight, so this blocks signed archive, validation, upload, and internal distribution. Resolution requires either an Individual/Organization Apple Developer Program enrollment for the repository owner's own Apple ID, or an upload-capable role on an authorized paid organization team. Both require user action; neither can be performed by an agent.
- `BLOCKED_BY_ACCOUNT_ROLE`: the same Apple ID additionally holds a `Marketing` seat in a separate organization whose Account Holder is a different person, and that organization has a pending updated Apple Developer Program License Agreement. `Marketing` grants no Certificates, Identifiers & Profiles access, which is why that organization never appears as a usable signing team in Xcode. Distributing TAPSO through that organization would require its Account Holder to accept the agreement and grant `Admin`, `App Manager`, or `Developer`, and would require the repository owner's explicit authorization to ship this product under that organization.
- `BLOCKED_BY_ENVIRONMENT`: the build host is critically low on disk. Measured 2026-08-21 after clearing this session's DerivedData: ~394 MB free of 228 GB. A Release archive plus DerivedData needs several GB, so the archive step will fail until space is freed. Simulator tests still pass, but `xcodebuild` already reported `No space left on device` while writing its `.xcresult` bundle.
- `UNVERIFIED`: physical-device Lock Screen, Dynamic Island, haptic, battery, and accessibility behavior. No physical device is registered to any available team.
- Backend matching mirrors core policy but is not generated from a shared cross-language specification; drift tests are needed.
- The app currently exposes the deterministic demo, not full stop/route search or vehicle-confirmation screens.
- Demo haptics occur only while the app is executing; no unsupported background haptic guarantee is made.
- Polling/caching and a durable ride-session worker are designed but not implemented.
- Local workspace folders managed by iCloud/FileProvider can introduce extended attributes or offloaded files that interfere with simulator signing; keep sources downloaded and use DerivedData outside the synced tree.
